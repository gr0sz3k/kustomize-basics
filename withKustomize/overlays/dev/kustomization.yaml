resources:
- ../../base

namespace: dev

patches:
- path: replicas.yaml

configMapGenerator:
- name: backend-config-map
  env: config.properties

images:
  - name: docker-repo/webapp
    newTag: latest